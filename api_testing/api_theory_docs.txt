# API - Application program interface between client and server.
# Everything is going to be  HTTP transfer, which is datga transfer based on request and response.
''' NOTE : while testing we have go to network in inspect section of web page.
Header > what is going on.
payload > what client given to us.
response > result from server
preview > It returns each results in response.

Development phase : Frontend dev > raise request to backend dev and get response then show in UI manner to US.
Tester : To make sure ene to end function that means frontend to backend is working fine.
  (FE give request to BE will provide response.How the FE is showing is called End to End testing).  '''

# simple GET request 
